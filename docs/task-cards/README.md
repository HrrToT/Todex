# 辅助模型任务卡

每个交给 GLM、DeepSeek 或 Qwen 的独立开发任务必须先创建一张 `T-NNN-*.md` 任务卡。任务卡至少包含：

- 目标与非目标
- 前置依赖和允许修改的文件范围
- 冻结的接口契约
- 先红后绿再重构的 TDD 要求
- 验收命令与预期结果
- 分支、commit 和 PR 提交格式
- 交付后的 spec 合规、代码质量和人工审查记录

任务卡将在正式 `PLAN.md` 生成后创建。

